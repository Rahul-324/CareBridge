spring:
  application:
    name: carebridge-backend

  datasource:
    url: jdbc:postgresql://localhost:5432/carebridge_db
    username: postgres
    password: rahul123
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
    properties:
      hibernate:
        format_sql: true

  flyway:
    enabled: true
    locations: classpath:db/migration

server:
  port: 8080

management:
  endpoints:
    web:
      exposure:
        include: health,info
